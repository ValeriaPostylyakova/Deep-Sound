import SidebarContainer from './SidebarContainer.tsx';

const Sidebar = () => {
    return (
        <aside className="sidebar">
            <SidebarContainer/>
        </aside>
    );
};

export default Sidebar;
